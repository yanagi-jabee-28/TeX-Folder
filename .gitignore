# 除外するフォルダ
.tmp.driveupload/
.tmp.drivedownload/

# LaTeX build outputs (keep PDFs under source control, ignore auxiliaries)


# Common LaTeX intermediate files
*.aux
*.bbl
*.blg
*.toc
*.lof
*.lot
*.idx
*.ind
*.ilg
*.fdb_latexmk
*.fls
*.synctex.gz
*.run.xml

# minted cache
_minted*/

# SVG intermediate files generated by svg package
# NOTE: Do not ignore SVGs per request (track PDFs/SVGs).

# Editor / OS
Thumbs.db
.DS_Store
